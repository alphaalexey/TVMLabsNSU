ReversePolishNotation {
    Expr
        = Expr Expr "+"  -- plus
        | Expr Expr "*"  -- times
        | number         -- num

    nonZeroDigit = "1".."9"

    number
        = "0"                  -- zero
        | nonZeroDigit digit*  -- nz
}
